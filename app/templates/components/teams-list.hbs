{{#if teams.length}}
  Mesitis currently has {{teams.length}} teams set up.
  <br>
  <br>
  <div class='card-columns'>
    {{#each teams as |team|}}
      <div class="card">
        <div class="card-block">
          <h4 class="card-title">{{team.name}}</h4>
          <p class="card-text">{{team.description}}</p>
          <p class="card-text"><small class="text-muted">{{team.count}} members</small></p>
        </div>
      </div>
    {{/each}}
    <div class="card">
      <div class="card-block">
        <h4 class="card-title"> Add new team? </h4>
        <p class="card-text">
          <form class="form-inline">
            <div class="form-group">
              <input type="text" class="form-control" placeholder="Ninja warriors">
            </div>
            <button type="submit" class="btn btn-primary"> Create </button>
          </form>
        </p>
        <p class="card-text">
          <small class="text-muted">
            Get a better picture of your member's availability, viewable by projects.
          </small>
        </p>
      </div>
    </div>
  </div>

{{else}}
  <div class="jumbotron">
    <h3>
      Looks like you haven't created any teams yet!
    </h3>
    <p>
      <br>
      You can organise your organisation's members into teams within Yalla.
      <br>
      This will help you get a better picture of your member's availability, viewable by projects.
    </p>
    <p>
      <br>
      <a class="btn btn-primary btn-lg" href="#" role="button">
        Create first team
      </a>
    </p>
  </div>
{{/if}}
