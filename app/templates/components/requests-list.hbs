<table class="table">
  <thead>
    <tr>
      <th> Requested By </th>
      <th> Dates </th>
      <th> Description </th>
      <th> Status </th>
    </tr>
  </thead>
  <tbody>
    {{#each requests as |request|}}
      <tr>
        <th>{{request.requestedBy}}</th>
        <td>
          {{request.from}} → {{request.from}}
          <small class="text-muted">
            <br>
            5 days
          </small>
        </td>
        <td>
          <span class="tag tag-pill tag-primary"> {{request.type}} </span>
          <small class="text-muted">
            <br>
            {{request.reason}}
          </small>
        </td>
        <td>{{request.status}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
