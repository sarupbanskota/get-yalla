Submit new leave request

<br>
<br>
<div class="alert alert-info" role="alert">
  Once you submit, your managers will be notified about your new leave request.
</div>

<form>
  <div class="form-group">
    {{pick-a-date class="form-control"
                  date=(readonly from)
                  on-selected=(action (mut from))
                  placeholder="From"
                  options=(readonly extraPickadateOptions)}}

  </div>
  <div class="form-group">
    {{pick-a-date class="form-control"
                  date=(readonly to)
                  on-selected=(action (mut to))
                  placeholder="To"
                  options=(readonly extraPickadateOptions)}}
  </div>
  <div class="form-group">
    {{textarea value=description
               class="form-control" rows="4"
               placeholder="Why're you requesting leave?"}}
  </div>
  <button type="submit" class="btn btn-primary"
          disabled={{incompleteRequest}}
          {{action 'createNewRequest'}}>
    Submit
  </button>
  {{#link-to 'requests'}}
    <button type="submit" class="btn btn-secondary">Cancel</button>
  {{/link-to}}
</form>
